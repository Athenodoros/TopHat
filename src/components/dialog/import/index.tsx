import { DialogImportFileScreen } from "./screens/file";

export const DialogImportView: React.FC = () => {
    // const page = useDialogState("import", state => state.page);

    return <DialogImportFileScreen />;
};
