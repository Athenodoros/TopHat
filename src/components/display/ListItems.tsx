import { ListItemText } from "@mui/material";
import { styled } from "@mui/material/styles";

export const PaddedListItemText = styled(ListItemText)({
    marginTop: "4px !important",
    marginBottom: "4px !important",
});
